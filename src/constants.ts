export const PAGE_HEIGHT = 700;
export const PAGE_GAP = 32;
export const ITEM_HEIGHT = PAGE_HEIGHT + PAGE_GAP; // 732 — one page slot in the virtual canvas
export const OVERSCAN = 2; // Buffer: 2 pages above, 2 pages below viewport
